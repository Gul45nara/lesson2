Дефекты в StringUtils:
1. Метод capitalize() не выбрасывает исключение при передаче None (ожидается TypeError).
2. Метод to_list() падает с AttributeError, если разделитель — None (ожидается более информативное исключение).